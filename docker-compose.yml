services:
  lstm-api:
    build: .
    ports:
      - "8280:8000"
    volumes:
      - ./data:/app/data
      - ./results:/app/results
      - /home/grva/AutoinspectionPath:/app/autoinspection
    restart: unless-stopped
