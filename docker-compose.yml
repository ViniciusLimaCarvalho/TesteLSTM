services:
  lstm-api:
    build: .
    ports:
      - "8280:8000"
    volumes:
      - ./data:/app/data
      - ./results:/app/results
      - /home/grva/Documents/imagens_convertidas/prediction:/app/prediction
    restart: unless-stopped
